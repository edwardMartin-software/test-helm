apiVersion: v1
kind: Secret
metadata:
  name: {{ include "sample-python-app.fullname" . }}
  labels:
    {{- include "sample-python-app.labels" . | nindent 4 }}
data:
  INSTRUMENTATION_KEY: {{ .Values.appInsights.instrumentationKey | b64enc | quote }}
